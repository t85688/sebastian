/******************************************************************************
** moxaclassbased_syncdeviceconfigstatemachinetypebase.cpp
**
**   **************************** auto-generated ****************************
**     This code was generated by a tool: UaModeler
**     Runtime Version: 1.8.0, using C++ OPC UA SDK 1.8.2 template (version 4)
**
**     Changes to this file may cause incorrect behavior and will be lost if
**     the code is regenerated.
**   **************************** auto-generated ****************************
**
** Copyright (c) 2006-2025 Unified Automation GmbH. All rights reserved.
**
** Software License Agreement ("SLA") Version 2.8
**
** Unless explicitly acquired and licensed from Licensor under another
** license, the contents of this file are subject to the Software License
** Agreement ("SLA") Version 2.8, or subsequent versions
** as allowed by the SLA, and You may not copy or use this file in either
** source code or executable form, except in compliance with the terms and
** conditions of the SLA.
**
** All software distributed under the SLA is provided strictly on an
** "AS IS" basis, WITHOUT WARRANTY OF ANY KIND, EITHER EXPRESS OR IMPLIED,
** AND LICENSOR HEREBY DISCLAIMS ALL SUCH WARRANTIES, INCLUDING WITHOUT
** LIMITATION, ANY WARRANTIES OF MERCHANTABILITY, FITNESS FOR A PARTICULAR
** PURPOSE, QUIET ENJOYMENT, OR NON-INFRINGEMENT. See the SLA for specific
** language governing rights and limitations under the SLA.
**
** The complete license agreement can be found here:
** http://unifiedautomation.com/License/SLA/2.8/
**
** Project: C++ OPC Server SDK information model for namespace http://www.moxa.com/auto-configuration-tool-class-based-model/
**
** Description: OPC Unified Architecture Software Development Kit.
**
******************************************************************************/

#include "moxaclassbased_syncdeviceconfigstatemachinetypebase.h"
#include "moxaclassbased_syncdeviceconfigstatemachinetype.h"
#include "uagenericnodes.h"
#include "nodemanagerroot.h"
#include "methodhandleuanode.h"
#include "moxaclassbased_nodemanagermoxaclassbasedns.h"

#include "instancefactory.h"
#include "opcua_extensionobject.h"
#include "opcua_finitestatevariabletype.h"
#include "opcua_finitetransitionvariabletype.h"
// Namespace for the UA information model http://www.moxa.com/auto-configuration-tool-class-based-model/
namespace MoxaClassBased {

bool SyncDeviceConfigStateMachineTypeBase::s_typeNodesCreated = false;
OpcUa::BaseMethod* SyncDeviceConfigStateMachineTypeBase::s_pStartMethod = NULL;
OpcUa::BaseMethod* SyncDeviceConfigStateMachineTypeBase::s_pStopMethod = NULL;

/** Constructs an SyncDeviceConfigStateMachineType object using an instance declaration node as base
*/
SyncDeviceConfigStateMachineTypeBase::SyncDeviceConfigStateMachineTypeBase(const UaNodeId& nodeId, UaObject* pInstanceDeclarationObject, NodeManagerConfig* pNodeConfig, UaMutexRefCounted* pSharedMutex)
: MoxaClassBased::ProgramStateMachineType(nodeId, pInstanceDeclarationObject, pNodeConfig, pSharedMutex)
{
    initialize();
}

/** Constructs an SyncDeviceConfigStateMachineType object
*/
SyncDeviceConfigStateMachineTypeBase::SyncDeviceConfigStateMachineTypeBase(const UaNodeId& nodeId, const UaString& name, OpcUa_UInt16 browseNameNameSpaceIndex, NodeManagerConfig* pNodeConfig, UaMutexRefCounted* pSharedMutex)
: MoxaClassBased::ProgramStateMachineType(nodeId, name, browseNameNameSpaceIndex, pNodeConfig, pSharedMutex)
{
    initialize();
}

/**  Constructs an instance of the class SyncDeviceConfigStateMachineTypeBase with all components
*/
SyncDeviceConfigStateMachineTypeBase::SyncDeviceConfigStateMachineTypeBase(
    UaBase::Object*    pBaseNode,    //!< [in] The "tree" of nodes that specifies the attributes of the instance and its children
    XmlUaNodeFactoryManager* pFactory, //!< [in] The factory to create the children
    NodeManagerConfig* pNodeConfig,  //!< [in] Interface pointer to the NodeManagerConfig interface used to add and delete node and references in the address space
    UaMutexRefCounted* pSharedMutex) //!< [in] Shared mutex object used to synchronize access to the variable. Can be NULL if no shared mutex is provided
: MoxaClassBased::ProgramStateMachineType(pBaseNode, pFactory, pNodeConfig, pSharedMutex)
{
    UaStatus      addStatus;

    if ( s_typeNodesCreated == false )
    {
        createTypes();
    }

    m_pStartMethod = NULL;
    m_pStopMethod = NULL;

    std::list<UaBase::BaseNode*> lstReferencedNodes = pBaseNode->hierarchicallyReferencedNodes();
    for (std::list<UaBase::BaseNode*>::const_iterator it = lstReferencedNodes.begin(); it != lstReferencedNodes.end(); it++)
    {
        UaBase::BaseNode *pChild = *it;
        if (s_pStartMethod && pChild->browseName() == s_pStartMethod->browseName())
        {
            if (!m_pStartMethod)
            {
                m_pStartMethod = (OpcUa::BaseMethod*) pFactory->createMethod((UaBase::Method*)pChild, pNodeConfig, pSharedMutex);
                addStatus = pNodeConfig->addNodeAndReference(this, m_pStartMethod, OpcUaId_HasComponent);
                UA_ASSERT(addStatus.isGood());
                if (!((UaBase::Method*)pChild)->modellingRuleId().isNull())
                {
                    m_pStartMethod->setModellingRuleId(((UaBase::Method*)pChild)->modellingRuleId());
                }
            }
        }
        else if (s_pStopMethod && pChild->browseName() == s_pStopMethod->browseName())
        {
            if (!m_pStopMethod)
            {
                m_pStopMethod = (OpcUa::BaseMethod*) pFactory->createMethod((UaBase::Method*)pChild, pNodeConfig, pSharedMutex);
                addStatus = pNodeConfig->addNodeAndReference(this, m_pStopMethod, OpcUaId_HasComponent);
                UA_ASSERT(addStatus.isGood());
                if (!((UaBase::Method*)pChild)->modellingRuleId().isNull())
                {
                    m_pStopMethod->setModellingRuleId(((UaBase::Method*)pChild)->modellingRuleId());
                }
            }
        }
    }
    UA_ASSERT(m_pStartMethod);
    UA_ASSERT(m_pStopMethod);
}

/** Initialize the object with all member nodes
*/
void SyncDeviceConfigStateMachineTypeBase::initialize()
{
    UaStatus      addStatus;

    if ( s_typeNodesCreated == false )
    {
        createTypes();
    }
    m_pStartMethod = NULL;
    m_pStopMethod = NULL;
    // Method Start
    m_pStartMethod = new OpcUa::BaseMethod(this, s_pStartMethod, m_pSharedMutex);
    addStatus = m_pNodeConfig->addNodeAndReference(this, m_pStartMethod, OpcUaId_HasComponent);
    UA_ASSERT(addStatus.isGood());

    // Method Stop
    m_pStopMethod = new OpcUa::BaseMethod(this, s_pStopMethod, m_pSharedMutex);
    addStatus = m_pNodeConfig->addNodeAndReference(this, m_pStopMethod, OpcUaId_HasComponent);
    UA_ASSERT(addStatus.isGood());

}

/** Destruction
*/
SyncDeviceConfigStateMachineTypeBase::~SyncDeviceConfigStateMachineTypeBase()
{
}

/** Create the related type nodes
*/
void SyncDeviceConfigStateMachineTypeBase::createTypes()
{
    if ( s_typeNodesCreated == false )
    {
        s_typeNodesCreated = true;

        // Check if supertype is already created
        MoxaClassBased::ProgramStateMachineType::createTypes();

        UaStatus      addStatus;
        UaVariant     defaultValue;
        NodeManagerRoot* pNodeManagerRoot = NodeManagerRoot::CreateRootNodeManager();
        OpcUa_Int16 nsTypeIdx = NodeManagerMoxaClassBasedNS::getTypeNamespace();
        OpcUa_Int16 nsSuperTypeIdx = MoxaClassBased::NodeManagerMoxaClassBasedNS::getTypeNamespace();
        NodeManagerConfig* pTypeNodeConfig = pNodeManagerRoot->getNodeManagerByNamespace(nsTypeIdx)->getNodeManagerConfig();

        OpcUa::GenericObjectType* pObjectType;
        pObjectType = new OpcUa::GenericObjectType(
            UaNodeId(MoxaClassBasedId_SyncDeviceConfigStateMachineType, nsTypeIdx),
            UaQualifiedName("SyncDeviceConfigStateMachineType", nsTypeIdx),
            UaLocalizedText("", "SyncDeviceConfigStateMachineType"),
            UaLocalizedText("", ""),
            OpcUa_False,
            &SyncDeviceConfigStateMachineType::clearStaticMembers);
        addStatus = pTypeNodeConfig->addNodeAndReference(UaNodeId(MoxaClassBasedId_ProgramStateMachineType, nsSuperTypeIdx), pObjectType, OpcUaId_HasSubtype);
        UA_ASSERT(addStatus.isGood());
        // ------------------------------------------------------------------------
        // Method Start
        // ------------------------------------------------------------------------
        s_pStartMethod = new OpcUa::BaseMethod(UaNodeId(MoxaClassBasedId_SyncDeviceConfigStateMachineType_Start, nsTypeIdx), "Start", MoxaClassBased::NodeManagerMoxaClassBasedNS::getTypeNamespace());
        s_pStartMethod->setModellingRuleId(OpcUaId_ModellingRule_Mandatory);
        addStatus = pTypeNodeConfig->addNodeAndReference(pObjectType, s_pStartMethod, OpcUaId_HasComponent);
        UA_ASSERT(addStatus.isGood());

        // ------------------------------------------------------------------------
        // Method Stop
        // ------------------------------------------------------------------------
        s_pStopMethod = new OpcUa::BaseMethod(UaNodeId(MoxaClassBasedId_SyncDeviceConfigStateMachineType_Stop, nsTypeIdx), "Stop", MoxaClassBased::NodeManagerMoxaClassBasedNS::getTypeNamespace());
        s_pStopMethod->setModellingRuleId(OpcUaId_ModellingRule_Mandatory);
        addStatus = pTypeNodeConfig->addNodeAndReference(pObjectType, s_pStopMethod, OpcUaId_HasComponent);
        UA_ASSERT(addStatus.isGood());


    }
}

/** Clear the static members of the class
*/
void SyncDeviceConfigStateMachineTypeBase::clearStaticMembers()
{
    s_typeNodesCreated = false;

    s_pStartMethod = NULL;
    s_pStopMethod = NULL;
}

/** Returns the type definition NodeId for the SyncDeviceConfigStateMachineType
*/
UaNodeId SyncDeviceConfigStateMachineTypeBase::typeDefinitionId() const
{
    UaNodeId ret(MoxaClassBasedId_SyncDeviceConfigStateMachineType, NodeManagerMoxaClassBasedNS::getTypeNamespace());
    return ret;
}

/** Implementation of the MethodManager interface method beginCall
*/
UaStatus SyncDeviceConfigStateMachineTypeBase::beginCall(
    MethodManagerCallback* pCallback,
    const ServiceContext&  serviceContext,
    OpcUa_UInt32           callbackHandle,
    MethodHandle*          pMethodHandle,
    const UaVariantArray&  inputArguments)
{
    UaStatus            ret;

    OpcUa::MethodCallJob* pCallJob = new OpcUa::MethodCallJob;
    pCallJob->initialize(this, pCallback, serviceContext, callbackHandle, pMethodHandle, inputArguments);
    ret = NodeManagerRoot::CreateRootNodeManager()->pServerManager()->getThreadPool()->addJob(pCallJob);
    if ( ret.isBad() )
    {
        delete pCallJob;
    }

    return ret;
}

/** Synchronous version of the MethodManager method call executed in a worker thread
*/
UaStatus SyncDeviceConfigStateMachineTypeBase::call(
    const ServiceContext&  serviceContext,
    MethodHandle*          pMethodHandle,
    const UaVariantArray&  inputArguments,
    UaVariantArray&        outputArguments,
    UaStatusCodeArray&     inputArgumentResults,
    UaDiagnosticInfos&     inputArgumentDiag)
{
    UaStatus            ret;
    MethodHandleUaNode* pMethodHandleUaNode = (MethodHandleUaNode*)pMethodHandle;
    UaMethod*           pMethod             = NULL;

    if (pMethodHandleUaNode)
    {
        pMethod = pMethodHandleUaNode->pUaMethod();

        if (pMethod)
        {
            if ( ( (m_pStartMethod != NULL) && (pMethod->nodeId() == m_pStartMethod->nodeId()) )
                 || ( (s_pStartMethod != NULL) && (pMethod->nodeId() == s_pStartMethod->nodeId()) ) )
            {
                if ( inputArguments.length() > 0 )
                {
                    ret = OpcUa_BadTooManyArguments;
                }
                else
                {
                    ret = this->Start(serviceContext);
                }

            }
            else if ( ( (m_pStopMethod != NULL) && (pMethod->nodeId() == m_pStopMethod->nodeId()) )
                      || ( (s_pStopMethod != NULL) && (pMethod->nodeId() == s_pStopMethod->nodeId()) ) )
            {
                if ( inputArguments.length() > 0 )
                {
                    ret = OpcUa_BadTooManyArguments;
                }
                else
                {
                    ret = this->Stop(serviceContext);
                }

            }
            else
            {
                return ProgramStateMachineType::call(serviceContext, pMethodHandle, inputArguments, outputArguments, inputArgumentResults, inputArgumentDiag);
            }
        }
        else
        {
            assert(false);
            ret = OpcUa_BadInvalidArgument;
        }
    }
    else
    {
        assert(false);
        ret = OpcUa_BadInvalidArgument;
    }

    return ret;
}

/** Returns the Start node.
  */
OpcUa::BaseMethod* SyncDeviceConfigStateMachineTypeBase::getStart()
{
    return m_pStartMethod;
}

/** Returns the Start node.
  */
const OpcUa::BaseMethod* SyncDeviceConfigStateMachineTypeBase::getStart() const
{
    return m_pStartMethod;
}
/** Returns the Stop node.
  */
OpcUa::BaseMethod* SyncDeviceConfigStateMachineTypeBase::getStop()
{
    return m_pStopMethod;
}

/** Returns the Stop node.
  */
const OpcUa::BaseMethod* SyncDeviceConfigStateMachineTypeBase::getStop() const
{
    return m_pStopMethod;
}

/** Uses the NodeAccessInfo of pOther and its children. */
void SyncDeviceConfigStateMachineTypeBase::useAccessInfoFromInstance(SyncDeviceConfigStateMachineTypeBase *pOther)
{
    MoxaClassBased::ProgramStateMachineType::useAccessInfoFromInstance(pOther);

    // children
    m_pStartMethod->useAccessInfoFrom(pOther->m_pStartMethod);
    m_pStopMethod->useAccessInfoFrom(pOther->m_pStopMethod);
}

/** Set the NodeAccessInfo for this node and all of its children. */
void SyncDeviceConfigStateMachineTypeBase::setAccessInfoWithChildren(NodeAccessInfo* pNodeAccessInfo)
{
    MoxaClassBased::ProgramStateMachineType::setAccessInfoWithChildren(pNodeAccessInfo);

    // children
    m_pStartMethod->setAccessInfoWithChildren(pNodeAccessInfo);
    m_pStopMethod->setAccessInfoWithChildren(pNodeAccessInfo);
}


} // End namespace for the UA information model http://www.moxa.com/auto-configuration-tool-class-based-model/


